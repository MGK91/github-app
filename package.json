{
  "name": "github-repo-metrics",
  "version": "1.0.0",
  "description": "Node.js application to fetch GitHub repository metrics like deployment frequency and lead time to changes",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "build": "rm -rf dist && mkdir dist && cp -r src dist && cp .env dist/ cp package.json dist/",
    "test": "jest",
    "test:unit": "jest tests/unit",
    "test:integration": "jest tests/integration"
  },
  "dependencies": {
    "axios": "^0.21.4",
    "dotenv": "^10.0.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "jest": "^27.0.0",
    "supertest": "^6.1.3"
  },
  "author": "Your Name",
  "license": "ISC"
}

